Contact form submissions are stored in the SQLite database created at:

`demo/assets/mail/data/contact_messages.sqlite`

The table name is `contact_messages`.

This file is created automatically the first time the contact form is submitted through:

`demo/assets/mail/contact-form.php`
